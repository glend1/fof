# railway.yml

name: my-java-app
services:
  web:
    buildCommand: mvn clean compile assembly:single
    runCommand: java -jar target/fof-0.1-jar-with-dependencies.jar

environments:
  production:
    secrets:
      - DISCORD_BOT_TOKEN